[build]
target = "mos-a800xl-none"
rustflags = "-C panic=abort" # -C link-args=-Wl,--oformat=elf"

[unstable]
build-std = ["core", "alloc"]

[target.mos-a800xl-none]
runner = ["/bin/echo", "can't run"]
